apiVersion: v1
kind: Service
metadata:
  name: reviews-service
  namespace: bookify
spec:
  type: NodePort
  selector:
    app: reviews-service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 30085

